/*const express = require('express');
const cors = require('cors');
const app = express();
const PORT = process.env.PORT || 4000;

app.use(cors());

app.get('/api/quotes', (req, res) => {
  res.json([{ text: 'Life is beautiful.', author: 'Unknown' }]);
});

app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}`);
});
*/